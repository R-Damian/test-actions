name: GitHub Actions Demo
on:  
  pull_request:
  push:
    branches:
      - master
  
jobs:
  example_matrix:
    runs-on: ["self-hosted"]
    steps:
      - run: echo mkdir D:\prueba-action
      - run: echo mkdir c:\prueba-action
